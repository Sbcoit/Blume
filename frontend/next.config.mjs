/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router is enabled by default in Next 14 when using the `app/` directory.
  // Add rewrites or other config here as your backend/API evolves.
};

export default nextConfig;



